# quince — progress dashboard

**One-line state.** ⚑ **The multi-storage epic is COMPLETE — `qn.6c` through `qn.6j` are all done,
`qn.6e` last to have its row written — and the frontier is `qn.6`, the v0.1 public shape (M5).**
The product is UNFROZEN (Operator ruling 2026-07-30, lifted on **risks, not on gates**: the ladder
was assessed grade by grade, quince-devlog#141/#146, and nothing on it blocked the decision). It was
hardware-proven over USB and Wi-Fi at `qn.6b` and runs under real daily use on staging. `qn.7` is
**done** — story 8 on hardware, 2026-07-31; what it still owes is in its row below.

**Live risks, carried into the unfreeze rather than gating it.** None is a gate. Each is something a
session meets and works around, named here so meeting one is recognised rather than rediscovered.

- **quince#202 — the largest, and it grows at the unfreeze.** `forge-watch` is structurally blind to
  the trunk, so `main` can be red with a healthy watch running; it was red for 4h40m and found by
  accident. Ruled 2026-07-30, **not built**.
- **quince-devlog#56** — a watch event names the last commenter and counts the rest.
- **G5 (watchdog) is unbuilt** — `stalled` is specified and not implemented; a gate that cannot be
  run cannot hold a door.
- **quince#32's proof is owed to an Operator re-provision window** — starting the arch service from
  a clean `conf.d` cannot be proven from a session the service hosts. **quince#33** wants a re-file.
  Both are unreadable to every identity (`quince-bot` is suspended, quince#173).
- **quince#54** — nothing detects drift between `loop-protocol.md` and the commands inline; two
  instances on 2026-07-30, both claim-level, so the gate that issue proposes would have passed both.

**Where the rest of this file went.** The narrative journal — 177 entries, and everything this
paragraph used to summarise — is now **one file per entry on the
[`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, which is never merged
into `main` and never protected. A default clone checks out `main` and the web UI shows `main`, so
that link is the only way to find it. Decisions live in [decisions/](decisions/), one file each. The
267-line state block that stood here until 2026-07-31 is preserved **verbatim** as a journal entry
rather than deleted — its own text argues that removing a criteria section on the day it is
satisfied leaves a ruling with no stated basis, and that argument is for keeping it citable, not for
keeping it here (quince-devlog#152, quince-devlog#30).

**What stays here is current state only:** the paragraph above, the per-rung dashboard below, and
the one open question. History does not. `bin/dashboard-size` fails when this file grows past the
threshold, so rotation is forced rather than remembered.

| Rung | Title | State |
| --- | --- | --- |
| qn.0 | Floor: scaffold, gates, CI, image | **done** — gates + image green in quince-dev (2026-07-19) |
| qn.1 | Core daemon skeleton + demo mode + UI shell | **done** — full gates + e2e + image green in quince-dev (2026-07-19) |
| qn.2 | muxd client + live device table | **done** — muxd client + registry + UI; `make gates`/image/e2e green (2026-07-20); lab gates 6–7 → owned by qn.2b |
| qn.2b | Muxer lifecycle + hardware proof (supervision, rescan, lab gate 7) | **done** — `internal/muxsup` supervisor + `POST /api/devices/rescan` + `devices.manage_muxer` + `/api/health` muxer + UI Rescan; `make gates`/image/e2e green + real-usbmuxd smoke test (2026-07-20); **lab gate 7 (managed USB + Rescan) PASSED on hardware**; gate 8 (netmuxd-USB audition) re-homed to qn.7 (aw), then split out of qn.7 to [quince#326](https://github.com/novkostya/quince/issues/326) (2026-07-31) |
| qn.3 | Device ops + Devices page | **done** — `internal/deviceops` (pair/validate/`ideviceinfo` + encryption via **pty**, never argv/env) + registry `Enrich` + enrichment driver + 4 frozen endpoints + `Op` lifecycle + audit + **pairing-record persistence** (amendment 1) + UI pair/encryption dialogs; `make gates`/image/e2e green (e2e story 3); coverage deviceops 80.2%, device 97.6%, httpapi 71.8%. **Lab gate 8 PASSED on hardware (2026-07-20)** — fresh container → **pair** (via UI, record persisted) → **recreate → still paired** (amendment 1 proven twice) → **change_password + disable→enable** cycle, all succeeding; **secrets proven** (`idevicebackup2 -i … {changepw,encryption off,encryption on}` — no password in argv, `BACKUP_PASSWORD` env count 0, clean logs). **4 findings fixed + CI-validated** (enrichment auto-pair on locked device; 3 UI) |
| qn.5 | Storage backends (zfs snapshot-native / reflink / hardlink / copy) + reconciliation | **done (CI-proven; landed `285c40b`..`3ce5bb1`)** — `internal/storage` (4 backends + auto-probe + journaled commit + `quince-version.json` markers + startup-reconciliation kill-matrix + adopted-version discovery + structural `Verify` (encryption-branched, A1) + `RepairWorkingCopy` + retention + the (bi)/(bk) **mirror ladder**: clone-from-`working/`, hook `mirror` verb → in-container reflink → hardlink-under-matrix → copy, surfaced/UNVERIFIED reporting) + `clonetree` (FICLONE/hardlink/copy) + `versions` registry + `DELETE /api/versions/{id}` + `version.*` events + reconcile-before-serve + `deploy/storage.md`; `make gates`/image/e2e green. **Proven in CI** (11 stories + reconciliation matrix + D5a anchored-filter contract) + **real-zfs commit/Verify on hardware** during the gate-12 investigation ((bf)→(bk)). **Lab gate 12's remaining hardware legs (host-side `mirror` verb, iMazing, syncoid, 12c destructive matrix) RE-HOMED to qn.4a** ((bm); named owner, legs preserved in the qn.5 spec). Ran BEFORE qn.4 (order ruled (ar)) |
| qn.4a | Backup engine + supervisor + minimal CLI (USB gate) | **built + landed (CI); gate 15 **FULLY hardware-proven** — ENGINE legs (bs) + zfs half (bw) + **iMazing-opens PASSED (cf)**** — `internal/backup` (state-machine engine + per-UDID single-flight + `idevicebackup2` streaming supervisor w/ the `<target>/<UDID>` **symlink adapter** + transcript-grounded parser + activity-sampler liveness w/ **A3** free-space watch + preflight + Seed→Verify→Commit/Discard + **startup job-row reconciliation**) + a `jobs` table/registry (real `JobReader`) + the job command surface (`POST /api/jobs` 202/409/422, `POST …/cancel`, `job.*` events) + the `quince backup` CLI (shared `buildLiveStack`); 6 lab transcripts extracted+scrubbed. `make gates`/image/e2e green; CI stories 1–14 incl. **wifi-torn→`connection_lost`** (a stall, not an error — sampler catches it), **verify-gate→`failed`**, **single-flight→409**, **startup-reconcile→`connection_lost`/rolled-forward-`succeeded`**. Coverage backup **83.2%** / store 80.8% / httpapi 72.2%. **Gate 15 split (clarified (bv)):** the ENGINE legs PASSED on real hardware (iPad, hardlink `/backups`) — CLI-USB backup both encryption variants (A1 encrypted `Verify` on real data), version rotation, interface facts 1+5, kill-matrix `backing_up`. The **zfs half is PROVEN ((bw))**: **engine→commit on the real zfs-hook backend** (encrypted, verified, version snapshot cut), host **`mirror` verb** + **`bclonesaved`** moving live (+~3 GB), **syncoid** mid-write (both `@quince-*` restore points + dirty `working/` replicated offsite) — the constrained forced-command hook key + `rbind,rslave` host→LXC→container propagation stood up on the real rpool; three deploy-doc hook bugs found+fixed (`$2`→last-arg, image-ssh-client, create-chown). Only **iMazing-opens** (Operator GUI) is unverified. **Landed on main.** |
| qn.4b | Wi-Fi first-class + transport policy + job-history UI (closes M3) | **built (CI-proven); lab gate 11/12c (hardware) pending** — transport **`auto` resolution** (prefer-USB-when-plugged, absent→**422** no job, concrete transport stored) + httpapi passes `auto` through; **`quince versions verify <id>\|--udid`** + **`device repair-working-copy <udid>`** CLI escape hatches (`storage.VerifyVersion`/`VerifyLatest`, browseRoot-resolved, no new backend surface); **live demo `JobControl`** (on-demand scripted jobs + seeded failed job for retry; single-flight; reverses qn.4a's 503); **UI** live Back up now (auto + transport override) / one-tap Retry on failed intent groups / Cancel on running job (details page + dashboard card). `make gates`/image/e2e green (e2e **story 4**: Back up now → cancel → retry). Retired the qn.4a Wi-Fi-success coverage finding (`wifi-incremental-success` story). Coverage backup **83.4%** / demo **55.3%** (was 0) / storage **78.2%** / httpapi 72.2% / cmd/quince 8.5% (CLI wiring hw-exercised). NOT a Wi-Fi demotion ((h) stands). **Lab gate 11 (both-transports UI-driven + honest Wi-Fi disconnect) + 12c (destructive hardlink matrix) = the consolidated hardware day with qn.4a gate 15**. **CLOSED (CI) 2026-07-20 ((by)):** its CI half is landed and complete; **gate 11 is RE-HOMED to `qn.4c`** (named owner — its Wi-Fi leg should run on SUPERVISED netmuxd, the shape actually deployed, not a hand-started one), **gate 12c is DEFERRED past the code freeze** (the destructive hardlink matrix gates a backend the Operator doesn't run — zfs deployment; the hardlink tier stays disabled-to-copy, surfaced), and findings (i)/(iv)/(v) **move to qn.4c**. No session work remains here. |
| qn.4c | **netmuxd supervision + usability fixes (the DAILY-DRIVER target)** | **DONE — CI-proven + LAB GATE 11 run on hardware ((ce)): 6/8 legs passed, (d) landed safely but mislabelled, (f)'s unencrypted half declared unrunnable with a reason.** Hardware: 33.3 GB full + cabled incremental committed over supervised netmuxd/usbmuxd, `auto`→USB proven, secrets clean on both transports, iMazing opened the committed `latest/` (retires qn.4a gate 15's last leg), cancel clean, verify+commit of 33 GB in 36 s, `bclonesaved` 46.5→80.1 GiB. A **gate-blocking** bug in landed qn.4a code was found AND fixed in-session ((cd)): the target stub on the cache filesystem made the device refuse any backup bigger than it. Seven findings filed. — `internal/muxsup` generalized to a daemon **`Spec`** (name/role/argv/probe-network/address) + **`Group`** (two daemons, one rescan) + the `plannedMuxers` resolution table; **netmuxd supervised** as `--host/--port --socket-path <private> --disable-usb` (every flag verified live; the private socket path is a SAFETY flag — netmuxd deletes and rebinds whatever socket it names, and its default is usbmuxd's: a silent USB blackout, reproduced then designed out, (bz)); `/api/health` **clean break** to `muxers:[{name,role,managed,state,detail,rescan}]`; rescan stays **USB-only**. Findings fixed: **(i)-A** `willEncrypt` exit-0-empty → `off` (`unknown` now means a real read failure), **(i)-B** preflight **re-reads encryption live** before refusing (cold-lockdown hard-fail gone; still-unknown refuses with the honest reason), **(v)** `last_backup` derived from the newest committed **version** (survives restarts, covers adopted, null `job_id`) + `AnnounceBackup` on commit success, **(iv)** verified **subsumed by running** (a new `DeviceCard` test proves the card already narrates verifying/committing). `make gates`/image/**e2e 6/6** green; **image smoke: both muxers `running`, `kill -9`d netmuxd respawned, usbmuxd socket intact**. Coverage muxsup **86.9%** / device 97.8% / backup 83.8% / cmd/quince 20.9%. Deploy: the **Wi-Fi mDNS constraint** is now first-class in `compose.nas.yml` (host-networking answer + honest security tradeoff + macvlan alternative), and P1b records the Wi-Fi twin of P1 ((ca)). **Lab gate 11 = the remaining hardware day.** History: inserted 2026-07-20 ((by)) to reach the Operator's "personally usable" bar before a planned code freeze. Scope: generalize the hardware-proven `internal/muxsup` to **co-supervise netmuxd** (config-gated, TCP probe vs its unix-socket one, restart-with-backoff, health surfaced — without it nothing starts netmuxd on `compose up`, so Wi-Fi dies silently after any restart: the qn.2b-for-usbmuxd reason, pulled forward from qn.7) + fix qn.4a findings **(i)** `willEncrypt`→`unknown` mis-map + the cold-lockdown race that hard-fails a legitimate encrypted backup at preflight, **(v)** the engine never writing `device.last_backup` (→ "No backups yet" on a device with real versions), **(iv)** the card lingering at "Backing up 100%" (likely subsumed by (v)). **Inherits qn.4b gate 11** — both transports UI-driven, live progress observed on a real backup, Wi-Fi over SUPERVISED netmuxd surviving a container restart, + the iMazing glance. Gate 12c stays deferred past the freeze. |
| qn.5b | **Atomic `latest` + `working/` lifecycle redesign** | **BUILT (CI-proven) 2026-07-24 ((cp)); spec approved-with-amendments ((co)).** `make gates` + `make image` green in `quince-dev` (backup 85.2% / storage 78.9% / httpapi 73.2%). **Atomic `latest`** via in-container `renameat2(RENAME_EXCHANGE)` (RENAME_EXCHANGE confirmed working on the CI tmpfs by the primitive test); **per-job `working/<udid>`** seeded from `latest/` via the backend's SAFE strategy (hardlink→copy, amendment A), kept dirty on FAILURE (retry resumes, proven no-re-transfer), removed on success; **commit reordered** verify→exchange→snapshot (marker-guarded resume across the non-idempotent exchange — kill-matrix green); **symlink dance DROPPED** (idevicebackup2 target = the storage `working/` parent → free-space bug 28b97de structurally impossible); **snapshot rename** `quince-<YYYY-MM-DDTHH-MM>-<ULID>` (amendment B: ULID kept); **honest internal `kind`** from the seed decision (finding #9(a)); **Reset** REST + CLI (accepted contract proposal). The two-observer gate is a CI concurrent-reader proof (`latest/` never missing/torn across a commit, both models). Hook `mirror`→`seed` verb; offsite filter drops `work/**`; canon (stack D5/D5a, design §4/§5, contracts §1/§2/§6, deploy/storage.md) updated. **Lab legs PROVEN on hardware 2026-07-24 ((ct)):** G-exchange-live/G-snapshot/G-rclone on the real rpool, Reset, keep-dirty-working, resume-without-re-seed, and **both** iPad (3 GB) + iPhone (34 GB) full cycles — plus a hardware-found seed-timeout bug fixed ((cs), branch `claude/qn5b-seed-timeout-fix`). Follow-ups RULED ((cv)): missing-versions UI ((cr)(a)) + seeding phase ((cu) opt 1) → qn.6a; continuous-reconcile ((cr)(b)) → epic (cl) constraint #7; **Finding B CLOSED ((cw), reviewed+landed (cy))** — `seed_in_progress` guard, legacy-safe, shared `prepareWorkDir` across both models; Wi-Fi failures are qn.7 (not qn.5b). **qn.5b FULLY closed**; latency mechanisms for (cu) parked evidence-gated ((cx)/(cz): gate patch dominates stand-in; in-process declined). Inserted 2026-07-22 ((cg)); ran **before the B2 cron is trusted**. Fixes the stack-D5 `PROPOSED (gap)`: both swap paths do `mv latest→latest.old; mv latest.new→latest`, so `latest/` briefly **doesn't exist** — an rclone sync crossing it **deletes the remote copy**. Scope: **exchange-rename** (`renameat2(RENAME_EXCHANGE)`, verified live on ZFS first; hook keeps the FICLONE reflink, quince does the privilege-free exchange); **per-job `working/`** seeded as a reflink clone of `latest/` at job start (between backups the dataset holds only `latest/` — snapshots structurally clean), **keeping the dirty `working/` on FAILURE so a retry resumes**; **commit reordered** to verify→exchange→snapshot so the version IS `latest/`; **the `<target>/<UDID>` symlink dance dropped** (it caused the free-space bug 28b97de); post-failure **Retry / Reset (± Retry-clean)** UX — 2-vs-3 actions is the implementer's call, landed as a contract proposal for review (`Reset` = the landed `RepairWorkingCopy`, CLI-only today). Collapses D5's two version models toward one. **Also inherits gate-11 finding #9(a) ((cj)): honest `full`-vs-`incremental` `kind` derived from whether `working/` was seeded from an existing `latest/` — the authoritative signal, replacing the unreliable `IsFullBackup` flag — and re-confirms the server single-`is_latest` invariant holds after the commit reorder.** |
| qn.6a | **Soak-ready UI (mobile + offline devices)** | **DONE — built, reviewed and landed 2026-07-24** (`main 3a7b068`); `make gates` + `image` + `gates-ui-e2e` green. Both ruled contract changes shipped: `missing: bool` on `wire.Version` and a **`seeding` STATE** (not a bare phase — the engine models lifecycle stages as states) between `preflight` and `backing_up`, both in contracts §2. Also: offline devices (migration `0004_device_identity`, persisted identity + last-seen), dead versions rendered explicitly dead, the log-blob `SplitFunc` fix, and the mobile-first responsive pass. Friction notes: `docs/specs/qn.6a/friction-notes.md`. **The last rung under the pre-revamp process** — its friction notes were the revamp's evidence. Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal), `(da)`/`(db)`/`(dc)`, 2026-07-24. |
| qn.6 | v0.1 release shape (after qn.7) | outlined |
| qn.6b | **Transport patience (LAST pre-freeze insert, (de))** | **LANDED 2026-07-24 ((df) build, (dj) review, main `3720f84`); LAB LEGS RUN 2026-07-25 ((dm)) — stories 9/10/11 validated on real hardware: candidate C (fast passcode + seed-during-gate on the zfs hook), the liveness patience (rode through a multi-minute device-side pause, no false kill), and kept-dirty-working RESUME-TO-COMPLETION all PROVEN; the bad-link `-4` (band-roam-induced TLS reset, unrescuable in-flight) is a well-scoped qn.7 item (auto-resume + `-4`→connection_lost). qn.6b lab debt CLEARED. Spec approved-with-amendments ((dg)/(dh)).** `make gates` + `make image` + `make gates-ui-e2e` green in `quince-dev` (backup 83.5% / deviceops 81.5% / storage 78.1%). **(1) Patched libimobiledevice built FROM SOURCE** at pinned `LIBIMOBILEDEVICE_REF=1.4.0` with two in-tree patches (`deploy/patches/libimobiledevice/`): `0001` raises the 30 s service receive timeouts → 15 min (#1413; `property_list_service.c`+`service.c`); `0002` adds `idevicebackup2 --gate <path>`. Only libimobiledevice is built (Alpine 3.24 ships the deps incl. the undocumented `libtatsu`); runtime `idevicebackup2 --help` lists `--gate`, the on-disk lib is `1.4.0-dirty` (the patched build wins over usbmuxd's soname-pulled apk copy), `-progs` dropped. **(2) Gate patch (candidate C):** the engine launches idevicebackup2 gated (passcode fires ~1–2 s), captures the fresh `Info.plist`, seeds `working/` in parallel (storage `Seed` split into `PrepareWork`/`SeedWork`), restores the `Info.plist` over the clone, opens the gate — proven end-to-end (`TestStoryGatedSeedOverlap`: committed version carries the FRESH `Info.plist`, passcode narrated during seeding; resume bypasses the gate). **(3) Liveness retune:** `LivenessTimeout` 15m → **18m** (= `toolReceiveTimeout` 15m + 3m margin) with a mechanical coupling guard — the sampler out-waits the tool's patience so a flap the tool rides out is never SIGKILLed, and (spike) a cleanly-idle dead link (tool loops `-5` forever) is classified only by the sampler. **Amendment A ((dg)):** the one unbounded non-backup tool op (`Manager.Validate`) bounded by `deviceOpTimeout` 30 s (interactive pair/encryption keep 2m/5m, both < 15m). **Item-4 verdict:** the captured Wi-Fi hang (`Could not receive (-4)`) was the tool's own exit (`outcomeProcErr` at ~44 s), NOT a sampler miss — a tool-patience defect item 1 targets; quince behaved correctly (kept dirty working). Contract changes: **NONE** (enum/state reused). Canon updated (stack D2, design §4 + the `seeding` state added to the state diagram, missing since qn.6a). Privacy swept clean (pcap fixtures local-only; patches infra-free). **Lab legs (were owed, now RUN — (dm)):** story 9 characterized (the `-4` boundary), stories 10/11 passed; candidate B not needed. Branch `claude/qn6b-*`, not pushed; architect lands ff-only via PR-CI. |
| qn.7 | **Wi-Fi sync from quince (device-ops)** | **DONE — story 8 passed on hardware 2026-07-31**, the rung's acceptance gate: a Wi-Fi backup end to end from a state quince itself created, driven from the iPad, no Mac involved. Rescoped by Operator ruling quince#325 to the flag alone — **reliability was DROPPED, not owed** (the drop is Apple-side; the remedy is `qn.12`), and the netmuxd-USB audition is split to quince#326 (**open**). The key is `EnableWifiConnections`, measured by WRITING it. **Still owed and NOT a gate:** the Operator's design call on quince#352 (disable does not belong on the device page). Six items `qn.6b` parked here are **homed** — quince#328, two dropped, four to `qn.6`. **Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, four entries dated 2026-07-31: the badge that took three hardware attempts, ten UI defects all found by clicking, and the hypothesis the roadmap told us to verify. |
| qn.6c | **Storage becomes plural** — the multi-storage epic's FIRST numbered rung | **CODE COMPLETE 2026-08-02 — 23 PRs, all four gaps ruled. THE RUNG IS NOT CLOSED: G9 is owed.** Operator-scoped [quince#378](https://github.com/novkostya/quince/issues/378); spec `docs/specs/qn.6c/qn.6c.md`, whose slicing table records what landed **through quince#461, plus quince#506** — the rung also landed [quince#490](https://github.com/novkostya/quince/pull/490), [quince#492](https://github.com/novkostya/quince/pull/492), [quince#495](https://github.com/novkostya/quince/pull/495), [quince#498](https://github.com/novkostya/quince/pull/498) (the **recheck button** the ruling assumed and nobody built, closing quince#459) and [quince#500](https://github.com/novkostya/quince/pull/500), which that table does not name. The four gap rulings (2026-07-31/08-01) are **decided text in canon** — `docs/contracts.md` and `docs/quince.design.md` — and should be read there, not restated here. `storage:` is now the LIST ([quince#506](https://github.com/novkostya/quince/pull/506), closing quince#473): no wrapper key, no global `backend`/`zfs`/`retention`, no inheritance, which makes quince#458 **unconstructible** rather than guarded. A single storage is just `- path: /backups` (quince#504). `backend: auto` stays legal. Retention is per-storage, so `Prune` groups a device's versions by storage; a version attributed to an undeclared storage is **skipped and logged**, never pruned under a number nobody wrote for it. **STILL OWED, and it is what keeps the rung open: G9** — a real device to two real storages, the second a genuine full transfer, **Operator, a lab day**. Everything to date is fixture roots and temp dirs, and the flatten added to what G9 must cover: two storages with *different* retention on one device did not exist before it. **Open follow-ups:** [quince#476](https://github.com/novkostya/quince/issues/476) (a `backend_mismatch` is unclearable and the refusal never says so — an unruled contracts/identity gap), [quince#505](https://github.com/novkostya/quince/issues/505). Out of scope and homed: storage cards (quince#443); add/remove a storage (`qn.6e`); external-readonly and import; the B2-as-replication fork; `zfs-native`; continuous reconciliation, which this rung **unblocks** by making *unreachable* and *artifact gone* separately representable. Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal), 2026-07-31 to 2026-08-02. |
| qn.6d | **Storage becomes visible** — cards on Home, a storage page, and Forget | **CODE COMPLETE 2026-08-03 — 12 PRs and every spec gate discharged. The CARDS are hardware-verified on the staging stand at `main@afcc6a1`, 2026-08-03 — capacity, counts and both storages, with the evidence on [quince#600](https://github.com/novkostya/quince/issues/600); **Forget was never exercised there**. The rung is not closed while open question 4 stands.** Operator-scoped [quince#443](https://github.com/novkostya/quince/issues/443); spec `docs/specs/qn.6d/qn.6d.md`. **Both gaps ruled 2026-08-03** and the ruled text is in `contracts.md` §1/§2, not restated here: gap A the field set as proposed, and **the card renders NO filesystem caveat** — two storages on one disk each show the same figure, a **ruled acceptance, not a bug to file**, because equal byte counts cannot prove a shared filesystem and both fields that would have carried filesystem identity were declined; gap B **Forget is a CONFIG MUTATION** — `DELETE /api/config/storage/{name}`, splicing server-side, `422` on the default, the restart **surfaced**, and recheck reporting runtime truth marked pending. **General config live-apply fell out of that reading as its own rung** ([quince#577](https://github.com/novkostya/quince/issues/577)) — the measured blocker is project-wide: `config.Service` has no `Apply`, `Reload` or `Subscribe` at all, so restart-to-apply is the status quo for **every** setting and D12 is already being deviated from systematically. **The rung's sharpest finding is not in its own code.** quince#593 read zfs capacity by sending flags through `hook_cmd` — **a forced command, which discards them and exits 0**, answering with the snapshot list. Every card read *"free space unavailable"* and no gate could have caught it: the tests stub the transport, so they assert the argv SENT and the stub answers whatever the test chose — *a mirror, not a peer*. Ruled and fixed as a **`capacity` verb taking no caller argument** ([quince#600](https://github.com/novkostya/quince/issues/600), [quince#602](https://github.com/novkostya/quince/pull/602)); `list` was deliberately **not** taught to forward flags, which would have dissolved *"dataset destroy impossible via the key"*. **It needed an operator migration** — the helper arm — and `deploy/storage.md` carries it. Two smaller instances of the same shape: `Card` dropped a `data-*` attribute that typechecked and never reached the DOM (TS does not check hyphenated JSX attributes), and `Progress` had no accessible role, so G1b's fill bar was unassertable. **STILL OWED: open question 4** — quince#570's HTTP claim has never been reproduced against a running daemon, and it needs an **authenticated session on the stand**, which no agent seat holds. `created_at` was **declined** ([quince#604](https://github.com/novkostya/quince/issues/604)); the spec stopped asking rather than the wire gaining a field. **No hardware gate, stated rather than implied** — unlike `qn.6c`'s G9, every gate here is CI or ui-e2e, and a real removable disk pulled and replaced is proven by none of them. |
| qn.6e | **adding a storage** — the probe, the form, and the first-run path | **DONE 2026-08-07 — 19 PRs merged, quince#683 closed, staging redeployed and verified.** Operator-ruled gap (option (a)): **any zero-storage start IS the onboarding state**, not only a fresh install. Three interface facts measured, **two of which contradicted the scoping**; `auto` is **absorbed, not removed**, and *"quince never writes `auto`"* was measured FALSE and corrected in contracts and spec. **The rung's own defect is the one it existed to prevent: the FIRST storage could never be added** — `Config.storage` is a pointer server-side, so a fresh install never reached the first-run page, and **six green unit tests missed it because every one seeded a storage first**. Found by building the container smoke that had twice been declared instead of built (quince#714); the e2e had gone green by fabricating the one shape that made it pass. **G4's host half is DISCHARGED 2026-08-10** — inside an image built from `main`, a bind-mounted host ZFS dir reads `f_type 0x2fc12fc1` while the image's own overlay root reads `0x794c7630`, so the constant `inspect.go` rests on is measured and discriminates. **Interface fact 2 (`zfs list` taking a PATH) is unmeasurable from any seat this project holds** — declared owed, not forgotten. **Open follow-ups:** quince#697 (`zfs.mode` defaults to `exec` and the image has no `zfs`), quince#709 (a second `internal/backup` flake), quince#716 (*"This is already a quince storage"* is true and reads as a refusal). **Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, 2026-08-07. |
| qn.6f | **quince serves TLS** — the listener, the certificate, and onboarding step 1 | **DONE 2026-08-02 — spec, four gaps and every slice merged the day the rung opened, and G7 PASSED the same evening.** A real iPhone completed onboarding on **both** tiers: the reverse-proxy one (Caddy, `X-Forwarded-Proto`) and a quince-served one (a real Let's Encrypt wildcard). Corroborated server-side rather than reported — both instances went `needs_setup` → `needs_login`, so first-run genuinely ran from the phone, with zero `ERROR` lines. **`OWED: G1`** — the `ui-e2e` for the zero-friction path; nothing automated exercises a secure origin because `--demo` cannot produce one, and the Operator's by-eye check on the proxy tier is worth more than nothing and is not a CI gate. **The root fact:** `secureCookie` gave plain-HTTP-to-a-non-loopback-host a `Secure` cookie the browser discards, so login looped with no error — and a phone is not loopback, so the primary client of a Wi-Fi backup tool could not log in. **What shipped:** the `426 insecure_origin` refusal ([quince#530](https://github.com/novkostya/quince/pull/530), closing [quince#497](https://github.com/novkostya/quince/issues/497)) and its link from the login form (#562); `tls:` keys (#535); the port on **`:8968`** (#538); `sessions.allow_insecure_transport` (#540); the **one-port** byte-sniffed listener with `CheckTLS`, rotation and the `301` (#551); G4/G5 pinned (#556); `deploy/tls.md` (#553); `GET /api/onboarding/https` (#554) and its page (#559). **Rulings are in canon, not restated here** — contracts §1/§6 and design §6 carry them, and [quince#446](https://github.com/novkostya/quince/issues/446) is where they were given. **The load-bearing one:** `Load()` falls back to `Default()` on a `Validate` failure, so the certificate check had to go on the SERVE path or a bad cert would start the daemon on plain HTTP for someone who asked for HTTPS. **Slice 7 (self-signed) is DROPPED, not deferred** — a click-through certificate forecloses service workers, so it would foreclose `qn.12` push; the page keeps a disabled *not implemented* row, provisionally, so **do not tidy it away and do not build toward it**. **Open follow-ups:** [#539](https://github.com/novkostya/quince/issues/539) the non-dismissible banner slice 8's ruling requires and did not get — until it lands, a user who enables the opt-in and never opens Settings sees nothing after startup; [#531](https://github.com/novkostya/quince/issues/531) a scoped gate ladder skips a gate silently; [#541](https://github.com/novkostya/quince/issues/541) nothing reports that two open PRs conflict. **The narrative — 20 PRs, and the session's recurring defect *a thing can run and still answer a narrower question than the one asked* — is on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, four entries dated 2026-08-02, rotated here rather than kept because this file is current state only. |
| qn.6g | **`config.yml` applies without a restart** — one propagation mechanism, storage first | **CODE COMPLETE 2026-08-07 — 7 PRs, every gate CI-green, and NO hardware gate owed** (declared up front, unlike `qn.6c`'s G9 and `qn.6d`'s question 4). Operator-scoped [quince#577](https://github.com/novkostya/quince/issues/577); spec `docs/specs/qn.6g/qn.6g.md`. **Not a storage rung** — `qn.6d` measured the blocker as project-wide: `config.Service` had no `Apply`/`Reload`/`Subscribe` at all. Ruled **one mechanism** on it, storage first, interim *"restart required"* notice **declined**. **Two gaps ruled:** file-watch → its own **unallocated** rung (option (a), 08-04), so a UI change applies immediately while a hand-edit still waits for a restart — that cost is in contracts §6 because it was the condition the ruling was taken on; in-flight forget → **`422` while a backup runs on that storage** (option (b), 08-06), (c) having been an amendment to ROLL-FORWARD rather than a footnote. **The deliverable is the per-key table** (contracts §6): **three bins, not two** — five keys are read by nothing, and calling those *restart-required* promises a restart fixes them. **Measured, not copied:** the spec's draft listed `backup.transport`, renamed and given a consumer by quince#654 **four days earlier**. **The sharpest finding is a gate that was TRUE while the behaviour was WRONG** — G5 (*"a busy storage is refused, the job named"*) was satisfied by checking liveness *before* the default refusal, so forgetting a default disk mid-backup said *wait for the backup*, which could never help. Every Go gate passed; `story8` caught it, because `--demo` keeps a job on its default storage. **G5c** asks what G5 did not. **Open follow-ups:** [quince#680](https://github.com/novkostya/quince/issues/680) the table has **no gate** — quince#493's shape one level up, and nothing fails if `devices.*` silently goes live; [quince#656](https://github.com/novkostya/quince/issues/656) `sessions.ttl_minutes` still read by nothing, published rather than fixed. **Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, 2026-08-06/07. |
| qn.6h | **zfs writes into the dataset root** — no seed, no working copy, no exchange; commit = verify → `zfs snapshot` | **DONE — MERGED AND PROVEN ON HARDWARE 2026-08-08**. A real backup wrote into a dataset root, verified, committed and snapshotted; reset rolled back; iMazing reads head and snapshot as ordinary iTunes backups. **A clean in-place version costs its DELTA — 140 MB where the old model cost 3.5 G on the same device.** Operator-ruled quince#591. **Reset = `rollback`**, refused while any newer snapshot exists — so excluding quince's datasets from the host snapshotter is **required setup** (quince#738), and a per-device quota is unsupported. Running it found four defects no gate could: quince#748, quince#749, quince#750, quince#751. |
| qn.6i | **reconciliation is asynchronous, triggered and never runs beside a commit** — startup, storage-added, scheduled | **CODE COMPLETE 2026-08-09 — all 6 PRs merged** ([quince#769](https://github.com/novkostya/quince/pull/769) spec, #771 the lease, #772 the runner, #774 the add trigger, #776 the schedule, #777 the UI indicator), every gate CI-green; **NO hardware gate owed** (declared at spec time). Operator-ruled [quince#731](https://github.com/novkostya/quince/issues/731), **both CLOSED**: [quince#715](https://github.com/novkostya/quince/issues/715) (an add hung the button ~48 s under `writeMu`) by a gate, and [quince#592](https://github.com/novkostya/quince/issues/592) (~36 s of connection-refused) **re-measured on the stand 2026-08-09** — the listener binds **1.6 ms** after the scan is triggered and the **19.7 s** of scanning runs behind it. The time did not go away; it moved. **The SPLIT is the rung**: roll-forward stays synchronous ahead of the listener, because `Engine.Reconcile` judges crash-orphaned job rows by asking whether a commit completed — moving it wholesale writes `connection_lost` for a backup that SUCCEEDED. Affordable because roll-forward is O(1) in tree size — two renames, or one snapshot — where the scan is not. Blocker 1 → a per-`(storage, device)` **lease**, the reconciler always defers and never blocks a commit; blocker 2 → **`reconciling` on `/api/health`**, contracts §1 stating a version list may be SHORT and that this is declared rather than empty. **Every guard measured RED by removing it.** **THREE SPEC CLAIMS WERE WRONG AND THE CODE WAS RIGHT** — the archive is a rename not a copy, `make demo` cannot measure G1, and G8's *"existing fixture"* never existed. **D4 is a confirmed seam property and an unconfirmed field event**, so this fixed no observed bug. **Both owed measurements DISCHARGED 2026-08-09 on the staging stand** — G1's wall-clock above, and the `reconciling` notice **seen by the Operator on a phone and observed clearing on its own**. **Still owed:** D4's production rate; the admin-CLI partial-pass guarantee (spec OQ7). `SweepWork` stays `return nil`; what it inherits is the lease. **Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal)**, 2026-08-09. |
| qn.6j | **`config.yml` contains only what was set** | **DONE 2026-08-09 — 7 PRs, every gate green, quince#728 CLOSED, and VERIFIED ON THE STAGING STAND.** Operator-ruled its own rung, pre-`v0.1`, 2026-08-08 on [quince#728](https://github.com/novkostya/quince/issues/728): the written file carries only the keys the user set, and **no generated annotation at all** — that half is CANCELLED, not deferred. Canon carries both in `docs/quince.stack.md` D12 (quince#752); spec `docs/specs/qn.6j/qn.6j.md` (quince#753). **Measured on a real container:** a hand-written declaration that returned 641 bytes returns the user's lines plus what they changed (60 → 80), and the first-run file is **60 bytes, was 292**. The mechanism is a **declared key set derived from the file at every load and never persisted** — the file is its own record — unioned with what each write changes; `omitempty` cannot deliver it, since the inflating keys are non-zero after `Resolved()`. Landed: quince#755 (`PUT /api/config` resolves before it validates, closing [quince#754](https://github.com/novkostya/quince/issues/754) — it refused the declaration quince's own startup error teaches and wrote `name: ""` into the running process), quince#756 (the wire-completeness guard, **before** the change it guards: no `json:` tag may carry `omitempty` **or `omitzero`**), quince#758, quince#759, quince#760. **One value quince must write that nobody set:** a lone storage's `default: true` is implied and unwritten, but the moment a second storage appears the incumbent's must be materialised or the next parse refuses the file. **A runtime round-trip guard** falls back to the full document with a named warning rather than shipping an unstartable config — **and its degradation is sticky across a restart, documented rather than fixed** (both detection mechanisms reintroduce the second source of truth the design refuses). **The preview is the file** — Operator ruling 2026-08-09 settled the last open question: `GET /api/config` gained `file_text`, read at request time and never cached, so the panel shows `config.yml` as it is on disk (quince#763). **The stand proved something no gate could:** with the preview live, the panel shows the FILE while the Settings editor holds a STALE draft — `useState(config)` never re-syncs — so one unrelated save silently reverted a hand-edit. That is [quince#764](https://github.com/novkostya/quince/issues/764), pre-existing and **made visible by this rung** rather than caused by it. Follow-up: [quince#761](https://github.com/novkostya/quince/issues/761) (no local command reproduces the `e2e` job). Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal), 2026-08-09. |
| qn.6p | **quince stops owning a muxer** — hardened is the only v0.1 profile; the operator runs the muxer and quince dials it | **CODE COMPLETE 2026-08-16 — 10 PRs, all merged, every gate CI-green. `G8` IS OWED TO HARDWARE and is declared, not claimed.** Operator-ruled on [quince#897](https://github.com/novkostya/quince/issues/897) ([confirmation](https://github.com/novkostya/quince/issues/897#issuecomment-5306693531), architect-relayed); spec `docs/specs/qn.6p/qn.6p.md`. **It began as four defects and became a change of profile:** quince#897 was filed after the hardened topology was RUN for the first time, and two of its four items dissolved once a config key means *an endpoint I dial* rather than *a daemon I own*. **What decided it was a trade disappearing** — Wi-Fi discovery is mDNS-only, so under the simple profile the muxer lived in quince's container and *quince* needed host networking (`compose.nas.yml` indicted itself for it); split out, the MUXER takes host networking and quince stays bridged, unprivileged, with no `/dev/bus/usb`. **The simple profile is DESCOPED, NOT ABANDONED** — `manage_muxer: true` is REFUSED at startup rather than deleted, because `yaml.Unmarshal` drops unknown keys silently and removing it would upgrade an all-in-one install into a muxerless one without a word; `muxsup`'s supervision is parked with its tests still running. **Landed:** #1055 one address grammar for **three** consumers, not the two quince#897 named — the third (`backup.socketAddr`) would have left presence and device ops working while every Wi-Fi BACKUP broke; #1057 health reports an external muxer from the connection quince actually holds, so `external` can no longer be claimed about an address that refused every connection; #1058 two keys naming one muxer open ONE connection; #1059 the refusal, on the SERVE path — a `Validate` error discards the config to `Default()`, which has no storage, so an upgrader would have been told to add their first storage ([quince#849](https://github.com/novkostya/quince/issues/849)); #1060 rescan RE-READS the muxer instead of restarting one (**PROVISIONAL** at the Operator's request); #1061 the image ships no muxer daemon; #1062 a read-only lockdown dir disables pairing with a reason instead of offering a button that fails after someone taps Trust; #1063 `deploy/compose.hardened.yml` + canon. **The rule the review produced:** a guard that fails LOUD may be declared untested; one that fails SILENT earns a test — twice a check existed, was tested, and was wired to nothing that would notice its absence. **Two findings outlive the rung:** `apk add usbmuxd` REPLACES quince's patched `libimobiledevice-1.0.so.6` with a symlink to the stock one, taking the 15-min receive patch with it (found by a size measurement coming out negative); and a readable `SystemConfiguration.plist` is indexed by netmuxd as a device that can shadow Wi-Fi lookups — measured NOT to bite at the pinned v0.4.3, which is why the shipped compose pins by DIGEST. **Owed:** `G8` (a real device over both transports through an external muxer, quince bridged and unprivileged); `Reread`'s close-under-`listen()` path, reasoned and not observed; the `manage_muxer: true` upgrade message as an operator sees it. **BLOCKED and deliberately unbuilt:** story 4 — what a single dual-transport muxer reports as `transport` does not reproduce from the code and needs the raw `/api/devices` output from quince#897's run. **Open:** whether `compose.nas.yml` becomes a NAS-flavoured copy of the hardened example or goes away. Narrative on the [`journal` branch](https://github.com/novkostya/quince-devlog/tree/journal), two entries 2026-08-16. |
| qn.8 | Vault: unlock, lazy browse, conformance suite | outlined |
| qn.9–10 | Domain viewers (overview / messages) | outlined |
| qn.11 | Photos viewer | **parked, lowest priority** (icloudpd+Immich cover photos; Apple-thumbnails spike first if revived) |
| qn.12 | PWA + push + schedules | outlined |


**Open questions for the Operator** (tracked here until resolved):
1. LAN registry port + creds (address recorded in `local/environment.md`; env-only,
   never committed).

*`qn.6d`'s two gaps stood here as questions 2 and 3 for nine hours on 2026-08-02/03. **Both were
ruled on 2026-08-03** — relayed by the architect seat on
[quince#443](https://github.com/novkostya/quince/issues/443) — and the ruled text is in canon
(`contracts.md` §1 and §2, both flipped from `PROPOSED (gap)` by the PR that also retired the
rung-ruled decision the ruling invalidated). **Gap A**: the fields land as proposed, all four
sub-questions as recommended — and the **card renders no filesystem caveat at all**, because equal
byte counts cannot prove a shared filesystem and both fields that would have carried filesystem
identity were declined. Two storages on one disk each show the same figure with nothing saying it is
the same space: a **ruled acceptance**, not a bug to file. **Gap B**: Forget is a **config
mutation** — `DELETE /api/config/storage/{name}` — with no live deregistration, the restart
surfaced, and recheck reporting runtime truth marked pending. **General config live-apply became its
own rung** out of the same reading —
[quince#577](https://github.com/novkostya/quince/issues/577), project-wide config→runtime
propagation with storage as its first consumer. The recommendations that stood here are not the
rulings; both were taken, which is not the same as being the record.*

*`qn.6f`'s three gaps stood here as questions 2–4 for six hours on 2026-08-02, and **all three were
ruled the same day they were filed** — relayed by architect session `arch1` on
[quince#446](https://github.com/novkostya/quince/issues/446). Plain HTTP is an explicit,
off-by-default, non-dismissibly-surfaced opt-in, and it **wins over the redirect**; one port serves
both protocols routed by the first byte, **vendored rather than `cmux`**; the default becomes
**`8968`**. A fourth question nobody had written down — **is onboarding step 1 pre-auth?** — was
found by an Operator question on quince#462, filed, and ruled inside the hour: yes, a fifth
`authExempt` route, **by exact path**. The ruled text is in canon (`design.md` §6, flipped by
quince#507; `contracts.md` §6's two blocks flip in the slices that build them) and is summarised in
the `qn.6f` row above. **The recommendations that stood here are not the rulings** — every one was
taken, which is not the same as being the record.*

*`qn.6c`'s four gaps stood here as questions 2–5 until 2026-08-02. **All four were ruled on
2026-07-31/08-01** — relayed by architect session `arch1` on
[quince#378](https://github.com/novkostya/quince/issues/378) — and seventeen PRs implemented them.
The ruled text is in canon (`contracts.md` §1/§2/§6, `design.md` §5, each flipped from `PROPOSED
(gap)` by the PR that built it) and summarised in the `qn.6c` row above; the recommendations that
stood here are NOT the rulings, and gap 3's was overruled outright.*

*Resolved questions, and the project-name/licence/owner rulings that stood here, are in the journal
entry for 2026-07-31 — a resolved question is history the moment it is answered.*
